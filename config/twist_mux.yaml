twist_mux:
  ros__parameters:
    topics:
      joystick:
        topic: /cmd_vel_joy_stamped
        timeout: 0.5
        priority: 100

      navigation:
        topic: /cmd_vel_nav_smooth_stamped
        timeout: 0.5
        priority: 10